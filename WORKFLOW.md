# 🔄 自动化工作流程

## 核心理念

**你只需要关心写文章，推送代码。其他一切自动完成。**

## 📝 日常工作流程

### 场景 1: 新增文章

```bash
# 1. 创建新文章
vim content/blog/rust-ownership.md

# 2. 本地预览（可选）
zola serve
# 访问 http://127.0.0.1:1111 查看效果

# 3. 提交并推送
git add content/blog/rust-ownership.md
git commit -m "新增文章：Rust 所有权系统"
git push

# 4. 完成！等待 2-3 分钟
# GitHub Actions 自动构建和部署
# 访问 https://leokang28.github.io 查看更新
```

### 场景 2: 修改现有文章

```bash
# 1. 编辑文章
vim content/blog/01-common-concepts.md

# 2. 提交并推送
git add content/blog/01-common-concepts.md
git commit -m "更新：优化 Common Concepts 文章"
git push

# 3. 完成！自动部署
```

### 场景 3: 修改网站配置

```bash
# 1. 修改配置
vim config.toml

# 2. 提交并推送
git add config.toml
git commit -m "配置：更新网站标题"
git push

# 3. 完成！自动部署
```

### 场景 4: 修改样式

```bash
# 1. 修改样式
vim static/css/custom.css

# 2. 提交并推送
git add static/css/custom.css
git commit -m "样式：优化归档页面样式"
git push

# 3. 完成！自动部署
```

### 场景 5: 批量操作

```bash
# 1. 同时修改多个文件
vim content/blog/article1.md
vim content/blog/article2.md
vim config.toml

# 2. 一次性提交
git add .
git commit -m "批量更新：3篇文章 + 配置调整"
git push

# 3. 完成！自动部署
```

## ⚡ 触发条件

GitHub Actions 会在以下情况自动触发：

✅ **任何文件推送到 main 分支**
- `.md` 文件（文章）
- `.toml` 文件（配置）
- `.css` 文件（样式）
- `.html` 文件（模板）
- 任何其他文件

✅ **手动触发**（可选）
- 访问 GitHub Actions 页面
- 点击 "Run workflow" 按钮

## 🔍 监控部署状态

### 查看实时进度

1. 推送代码后，访问：
   ```
   https://github.com/leokang28/leokang28.github.io/actions
   ```

2. 点击最新的工作流运行

3. 查看实时日志：
   ```
   ✓ Checkout repository
   ✓ Install Zola
   ✓ Build site
   ✓ Upload artifact
   ✓ Deploy to GitHub Pages
   ```

### 部署时间

| 步骤 | 时间 |
|------|------|
| 检出代码 | ~10 秒 |
| 安装 Zola | ~20 秒 |
| 构建网站 | ~30 秒 |
| 上传构建产物 | ~20 秒 |
| 部署到 Pages | ~30 秒 |
| **总计** | **~2 分钟** |

## 📧 通知设置（可选）

### 启用 Email 通知

1. 访问：https://github.com/settings/notifications
2. 勾选 **Actions**
3. 选择通知方式：
   - ✉️ Email
   - 🔔 Web 通知
   - 📱 移动通知

### 部署状态徽章

在 README.md 中添加：

```markdown
[![Deploy](https://github.com/leokang28/leokang28.github.io/actions/workflows/deploy.yml/badge.svg)](https://github.com/leokang28/leokang28.github.io/actions/workflows/deploy.yml)
```

显示效果：
- 🟢 绿色 = 部署成功
- 🔴 红色 = 部署失败
- 🟡 黄色 = 正在部署

## 🚫 什么不会触发部署

❌ 只修改但不推送
❌ 推送到非 main 分支（如 dev 分支）
❌ Pull Request（除非合并到 main）
❌ 标签推送（tags）

## 🎯 最佳实践

### 1. 频繁提交，小步快跑

```bash
# 好的做法：小改动，频繁推送
git commit -m "修改：修复拼写错误"
git push

# 不太好：积累太多改动
# 100+ 文件修改，一次性推送
```

### 2. 使用有意义的提交信息

```bash
# ✅ 好的提交信息
git commit -m "新增：Rust 生命周期详解"
git commit -m "修复：归档页面样式问题"
git commit -m "优化：首页加载性能"

# ❌ 不好的提交信息
git commit -m "update"
git commit -m "fix"
git commit -m "aaa"
```

### 3. 本地预览后再推送

```bash
# 1. 本地预览
zola serve

# 2. 确认无误后推送
git push
```

### 4. 使用 .gitignore

已配置的忽略文件：
```
public/          # 构建产物（自动生成）
.DS_Store        # macOS 系统文件
*.swp, *.swo     # Vim 临时文件
*~               # 备份文件
```

## 🐛 问题排查

### 部署失败怎么办？

1. **查看详细日志**
   ```
   GitHub Actions → 点击失败的运行 → 查看详细步骤
   ```

2. **常见问题**

   **构建失败**：
   ```bash
   # 本地测试构建
   zola build
   
   # 查看错误信息
   # 通常是 .md 文件格式问题或 config.toml 语法错误
   ```

   **部署失败**：
   - 检查 GitHub Pages 设置
   - 确认 Actions 权限正确

3. **回滚到上一个版本**
   ```bash
   git log --oneline  # 查看提交历史
   git revert HEAD    # 回滚最后一次提交
   git push           # 推送回滚
   ```

## 📊 工作流程可视化

```
┌─────────────────────────────────────────────────────────────┐
│  你的本地环境                                                 │
│  ┌─────────┐      ┌─────────┐      ┌─────────┐             │
│  │ 编辑文章 │ ---> │ git add │ ---> │git commit│             │
│  └─────────┘      └─────────┘      └─────────┘             │
│                                            │                 │
│                                            ↓                 │
│                                     ┌──────────┐             │
│                                     │ git push │             │
│                                     └──────────┘             │
└──────────────────────────────────────────│───────────────────┘
                                           │
                                           ↓
┌─────────────────────────────────────────────────────────────┐
│  GitHub (自动)                                               │
│  ┌──────────────────┐                                        │
│  │  检测到 push     │                                        │
│  │  触发 Actions    │                                        │
│  └────────┬─────────┘                                        │
│           │                                                  │
│           ↓                                                  │
│  ┌──────────────────┐                                        │
│  │  安装 Zola       │                                        │
│  └────────┬─────────┘                                        │
│           │                                                  │
│           ↓                                                  │
│  ┌──────────────────┐                                        │
│  │  zola build      │                                        │
│  └────────┬─────────┘                                        │
│           │                                                  │
│           ↓                                                  │
│  ┌──────────────────┐                                        │
│  │  部署到 Pages    │                                        │
│  └────────┬─────────┘                                        │
└───────────┼──────────────────────────────────────────────────┘
            │
            ↓
┌─────────────────────────────────────────────────────────────┐
│  leokang28.github.io (结果)                                 │
│  ┌──────────────────┐                                        │
│  │  网站自动更新    │  ✅                                    │
│  └──────────────────┘                                        │
└─────────────────────────────────────────────────────────────┘
```

## 🎉 总结

**你需要做的**：
1. 写文章/改代码
2. `git push`

**自动完成的**：
1. 构建
2. 测试
3. 部署
4. 更新网站

**就这么简单！** 🚀

